#include <stdio.h>

void main()

{
  int principle = 29000, rate = 7, year = 10;
  int simpleInterest = (principle * rate * year) / 100;
  printf("The value of simpleInterest is %d", simpleInterest);
}
